# Island Page Diagnostic (MVP UI)

Visual preview of a per-URL Island diagnostic report. Uses **mock data only** — it does not inspect a real browser session.

```bash
npm install
npm run dev
```

Open the local URL Vite prints (usually `http://localhost:5173`).
